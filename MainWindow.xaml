<Window x:Class="ZeroMix.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ZeroMix Dashboard"
        Icon="zeromix.ico"
        Height="450"
        Width="700"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Window.Resources>
        <!-- Modern Light Color Palette -->
        <Color x:Key="BackgroundColor">#F0F0F0</Color>
        <Color x:Key="ForegroundColor">#1C1C1C</Color>
        <Color x:Key="BorderColor">#DCDCDC</Color>
        <Color x:Key="AccentColor">#0078D4</Color>
        <Color x:Key="ControlBackgroundColor">#FFFFFF</Color>
        <Color x:Key="ControlHoverColor">#E0E0E0</Color>
        <Color x:Key="NavSelectedColor">#E2E2E2</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
        <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource ForegroundColor}" />
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
        <SolidColorBrush x:Key="ControlBackgroundBrush" Color="{StaticResource ControlBackgroundColor}" />
        <SolidColorBrush x:Key="ControlHoverBrush" Color="{StaticResource ControlHoverColor}" />
        <SolidColorBrush x:Key="NavSelectedBrush" Color="{StaticResource NavSelectedColor}" />

        <!-- Navigation Button Style -->
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4">
                            <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ControlHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MainButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Navigation Panel -->
        <Border Grid.Column="0" Background="{StaticResource ControlBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <Button Name="HomeButton" Content="Home" Click="HomeButton_Click" Style="{StaticResource NavButton}"/>
                <Button Name="DashboardButton" Content="Dashboard" Click="DashboardButton_Click" Style="{StaticResource NavButton}" Margin="0,5,0,0"/>
                <Button Name="CustomButton" Click="CustomButton_Click" Style="{StaticResource NavButton}" Margin="0,5,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚙️" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text=" Settings" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Right Content Area -->
        <Grid Grid.Column="1" Name="ContentArea">
            
            <!-- Home Content -->
            <StackPanel Name="HomeContent" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="16">
                <TextBlock Text="ZeroMix" FontSize="32" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <!-- <Button Width="220" Height="40" Click="OpenOverlay_Click" Style="{StaticResource MainButton}">Open Overlay</Button> -->
                <TextBlock Text="Press Alt+Space to open the overlay from anywhere." FontSize="14" Foreground="#888" TextAlignment="Center" Margin="0,16,0,0"/>
            </StackPanel>

            <!-- Dashboard Content (Initially Hidden) -->
            <Grid Name="DashboardContent" Visibility="Collapsed" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="System Dashboard" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,15"/>

                <!-- Performance Section -->
                <GroupBox Grid.Row="1" Header="Live Performance" FontWeight="SemiBold">
                    <StackPanel Margin="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="CPU Usage:" FontWeight="Normal"/>
                            <TextBlock Name="CpuUsageText" Grid.Column="1" Text="-- %" Margin="10,0,0,0" FontWeight="Normal"/>
                        </Grid>
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="RAM Usage:" FontWeight="Normal"/>
                            <TextBlock Name="RamUsageText" Grid.Column="1" Text="-- / -- MB" Margin="10,0,0,0" FontWeight="Normal"/>
                        </Grid>
                    </StackPanel>
                </GroupBox>

                <!-- Maintenance Section -->
                <GroupBox Grid.Row="2" Header="System Maintenance" FontWeight="SemiBold" Margin="0,15,0,0">
                    <StackPanel Margin="10">
                        <Button Name="CleanTempButton" Content="Clean Temporary Files" Click="CleanTempButton_Click" Style="{StaticResource MainButton}" HorizontalAlignment="Left"/>
                        <TextBlock Name="CleanStatusText" Text="" Margin="0,10,0,0" Foreground="#555" FontStyle="Italic"/>
                    </StackPanel>
                </GroupBox>
            </Grid>
        </Grid>
    </Grid>
</Window>