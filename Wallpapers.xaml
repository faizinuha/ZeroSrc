<Window
    x:Class="ZeroMix.Wallpapers"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Wallpaper Manager"
    Width="650"
    Height="1000"
    AllowsTransparency="True"
    Background="Transparent"
    Loaded="Window_Loaded"
    MouseLeftButtonDown="Window_MouseLeftButtonDown"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None">

    <Window.Resources>
        <!-- Anime Minimalist Palette -->
        <Color x:Key="PrimaryColor">#8BE9FD</Color>
        <Color x:Key="BackgroundColor">#161925</Color>
        <Color x:Key="CardColor">#21222C</Color>
        <Color x:Key="HeaderColor">#0F111A</Color>
        <Color x:Key="TextColor">#F8F8F2</Color>
        <Color x:Key="SubTextColor">#6272A4</Color>
        
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="CardBrush" Color="{StaticResource CardColor}"/>
        <SolidColorBrush x:Key="HeaderBrush" Color="{StaticResource HeaderColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="SubTextBrush" Color="{StaticResource SubTextColor}"/>

        <!-- Button Styles -->
        <Style x:Key="AnimeButton" TargetType="Button">
            <Setter Property="Background" Value="#282A36"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#44475A"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#383A59"/>
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryAnimeButton" BasedOn="{StaticResource AnimeButton}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D6BCFA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WallpaperBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderBrush" Value="#343746"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="20" CornerRadius="20" Background="{StaticResource BackgroundBrush}" BorderBrush="#343746" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" Opacity="0.4" ShadowDepth="0" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource HeaderBrush}" CornerRadius="20,20,0,0">
                <Grid Margin="30,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="12" Height="12" Fill="{StaticResource PrimaryBrush}" Margin="0,0,15,0"/>
                        <StackPanel>
                            <TextBlock Text="Wallpaper Manager" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                            <TextBlock Name="CountLabel" Text="0 items synchronized" FontSize="11" Foreground="{StaticResource SubTextBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button Name="CloseButton" Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center" 
                            Click="CloseButton_Click" Background="Transparent" Foreground="{StaticResource SubTextBrush}" 
                            BorderThickness="0" FontSize="18" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="#FF5555"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10,20">
                <ItemsControl Name="WallpaperListPanel">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource WallpaperBorderStyle}" MouseLeftButtonUp="WallpaperBorder_MouseLeftButtonUp">
                                <Grid>
                                    <Border CornerRadius="20" ClipToBounds="True">
                                        <Image Source="{Binding Thumbnail}" Stretch="UniformToFill" Height="180" Width="240"/>
                                    </Border>
                                    <Border VerticalAlignment="Bottom" Background="#AA0F111A" CornerRadius="0,0,20,20" Padding="12,10">
                                        <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextBrush}" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" Padding="30,25" Background="{StaticResource HeaderBrush}" CornerRadius="0,0,20,20">
                <Grid>
                    <TextBlock Name="StatusLabel" Text="Ready to harmonize your desktop" VerticalAlignment="Center" Foreground="{StaticResource SubTextBrush}" FontSize="13"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Name="BrowseButton" Content="Import Art" Click="BrowseButton_Click" Style="{StaticResource AnimeButton}" Margin="0,0,15,0"/>
                        <Button Name="SetWallpaperButton" Content="Apply Wallpaper" Click="SetWallpaperButton_Click" Style="{StaticResource PrimaryAnimeButton}" Width="160"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
