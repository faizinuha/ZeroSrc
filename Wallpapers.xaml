<Window
    x:Class="ZeroMix.Wallpapers"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Wallpaper Manager"
    Width="1000"
    Height="720"
    AllowsTransparency="True"
    Background="Transparent"
    Loaded="Window_Loaded"
    MouseLeftButtonDown="Window_MouseLeftButtonDown"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None">

    <Window.Resources>
        <!-- Modern Dark Theme -->
        <ResourceDictionary>
            <Color x:Key="AccentColor">#0A84FF</Color>
            <Color x:Key="ControlHoverColor">#2C2C2E</Color>
            <Color x:Key="ForegroundColor">#FFFFFF</Color>
            <Color x:Key="BackgroundColor">#1A1A1A</Color>
            <Color x:Key="SelectedColor">#0A84FF</Color>
            <Color x:Key="BorderColor">#3A3A3C</Color>
            <Color x:Key="SubTextColor">#8E8E93</Color>
            <Color x:Key="HeaderColor">#252525</Color>
            
            <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
            <SolidColorBrush x:Key="ControlHoverBrush" Color="{StaticResource ControlHoverColor}" />
            <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource ForegroundColor}" />
            <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
            <SolidColorBrush x:Key="SelectedBrush" Color="{StaticResource SelectedColor}" />
            <SolidColorBrush x:Key="BorderBrushColor" Color="{StaticResource BorderColor}" />
            <SolidColorBrush x:Key="SubTextBrush" Color="{StaticResource SubTextColor}" />
            <SolidColorBrush x:Key="HeaderBrush" Color="{StaticResource HeaderColor}" />

            <Style x:Key="BaseButton" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource AccentBrush}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="16,10" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.9" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.8" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.5" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="SecondaryButton" BasedOn="{StaticResource BaseButton}" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource ControlHoverBrush}" />
                <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
            </Style>

            <Style x:Key="CloseButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="#8E8E93" />
                <Setter Property="Width" Value="46" />
                <Setter Property="Height" Value="46" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="Transparent" CornerRadius="23">
                                <Path x:Name="xpath" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M0,0 L12,12 M12,0 L0,12" Stroke="{TemplateBinding Foreground}" StrokeThickness="2" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#E81123" />
                                    <Setter TargetName="xpath" Property="Stroke" Value="White" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="WallpaperImageStyle" TargetType="Image">
                <Setter Property="Width" Value="180" />
                <Setter Property="Height" Value="120" />
                <Setter Property="Stretch" Value="UniformToFill" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality" />
                <Setter Property="RenderOptions.EdgeMode" Value="Aliased" />
            </Style>

            <Style x:Key="WallpaperBorderStyle" TargetType="Border">
                <Setter Property="BorderBrush" Value="#3A3A3C" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Margin" Value="8" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Background" Value="#252525" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                        <Setter Property="BorderThickness" Value="2" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="SelectedBorderStyle" TargetType="Border" BasedOn="{StaticResource WallpaperBorderStyle}">
                <Setter Property="BorderBrush" Value="{StaticResource SelectedBrush}" />
                <Setter Property="BorderThickness" Value="3" />
                <Setter Property="Background" Value="#0A84FF22" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Margin="12" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Opacity="0.6" ShadowDepth="0" Color="Black" />
        </Border.Effect>
        <Border Background="#1A1A1A" BorderBrush="#2C2C2E" BorderThickness="1" CornerRadius="12" ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="60" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Title Bar -->
                <Border Grid.Row="0" Background="#252525">
                    <Grid>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="24,0,0,0">
                            <TextBlock Text="🖼️" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="Wallpaper Manager" FontSize="18" FontWeight="SemiBold" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                            <TextBlock Name="CountLabel" Text="0 items" VerticalAlignment="Center" FontSize="12" Foreground="#8E8E93" Margin="20,0,0,0" />
                        </StackPanel>
                        <Button Name="CloseButton" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0" Click="CloseButton_Click" Style="{StaticResource CloseButton}" ToolTip="Close" />
                    </Grid>
                </Border>

                <!-- Wallpaper Grid (Simplified - No Search/Filter) -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Background="#1A1A1A">
                    <ItemsControl Name="WallpaperListPanel">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource WallpaperBorderStyle}" MouseLeftButtonUp="WallpaperBorder_MouseLeftButtonUp">
                                    <Grid>
                                        <Image Source="{Binding Thumbnail}" Style="{StaticResource WallpaperImageStyle}" />
                                        <Border VerticalAlignment="Bottom" Background="#DD000000" CornerRadius="0,0,8,8">
                                            <TextBlock Text="{Binding Name}" Margin="10,6" Foreground="White" FontSize="11" FontWeight="Medium" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="4" Margin="16,8" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Action Footer -->
                <Border Grid.Row="2" Background="#252525" Padding="24,18">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Status Label -->
                        <TextBlock Grid.Column="0" Name="StatusLabel" Text="Select a wallpaper to preview" FontSize="13" Foreground="#8E8E93" VerticalAlignment="Center" />

                        <!-- Action Buttons -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Name="BrowseButton" Content="📂 Browse..." Click="BrowseButton_Click" Style="{StaticResource SecondaryButton}" Padding="16,10" Margin="0,0,12,0"/>
                            <Button Name="SetWallpaperButton" Content="✓ Set as Wallpaper" Click="SetWallpaperButton_Click" Style="{StaticResource BaseButton}" Padding="20,10" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Border>
</Window>
