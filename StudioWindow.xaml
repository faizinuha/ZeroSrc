<Window x:Class="ZeroMix.StudioWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ZeroMix Studio" Height="600" Width="900" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <!-- Minimalist Palette -->
        <SolidColorBrush x:Key="BgBrush" Color="#0B0D11"/>
        <SolidColorBrush x:Key="SidebarBrush" Color="#12151C"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#00FFA3"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#1F2430"/>
        <SolidColorBrush x:Key="TextBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="SubTextBrush" Color="#94A3B8"/>

        <!-- Simple Button Style -->
        <Style x:Key="StudioBtn" TargetType="Button">
            <Setter Property="Background" Value="#1A1F2B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#252B3B"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ExportBtn" BasedOn="{StaticResource StudioBtn}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BgBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/> <!-- sidebar tools -->
                <ColumnDefinition Width="*"/>   <!-- preview area -->
            </Grid.ColumnDefinitions>

            <!-- Tools Sidebar -->
            <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0" Padding="20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="ZERO MIX STUDIO" FontSize="14" FontWeight="Black" Margin="0,0,0,5" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text="Simple Video Engine" FontSize="10" Foreground="{StaticResource SubTextBrush}" Margin="0,0,0,25"/>

                        <!-- Basic Edits -->
                        <TextBlock Text="BASIC CONFIGURATION" FontSize="9" FontWeight="Bold" Foreground="{StaticResource SubTextBrush}" Margin="0,0,0,10"/>
                        
                        <TextBlock Text="Volume Control" FontSize="11" Foreground="{StaticResource TextBrush}" Margin="0,10,0,5"/>
                        <Slider x:Name="VolumeSlider" Minimum="0" Maximum="200" Value="100" Margin="0,0,0,15"/>

                        <TextBlock Text="Resolution Scale" FontSize="11" Foreground="{StaticResource TextBrush}" Margin="0,5,0,5"/>
                        <ComboBox x:Name="ResSelector" SelectedIndex="0" Background="#1A1F2B" Foreground="White" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}" Margin="0,0,0,15">
                            <ComboBoxItem Content="Current (Original)"/>
                            <ComboBoxItem Content="FHD (1080p)"/>
                            <ComboBoxItem Content="HD (720p)"/>
                            <ComboBoxItem Content="SD (480p)"/>
                        </ComboBox>

                        <!-- Visuals -->
                        <TextBlock Text="VISUAL &amp; AUDIO" FontSize="9" FontWeight="Bold" Foreground="{StaticResource SubTextBrush}" Margin="0,20,0,10"/>
                        
                        <TextBlock Text="Watermark Studio" FontSize="11" Foreground="{StaticResource TextBrush}" Margin="0,5,0,5"/>
                        <ComboBox x:Name="WatermarkTypeSelector" SelectedIndex="0" Background="#1A1F2B" Foreground="White" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}" Margin="0,0,0,5">
                            <ComboBoxItem Content="No Watermark" Tag="None"/>
                            <ComboBoxItem Content="ZeroMix Logo" Tag="Default"/>
                            <ComboBoxItem Content="Custom Image/Text" Tag="Custom"/>
                        </ComboBox>
                        <Button x:Name="CustomWatermarkBtn" Content="Upload My Brand..." Style="{StaticResource StudioBtn}" HorizontalAlignment="Stretch" Margin="0,0,0,15" Visibility="Collapsed" Click="CustomWatermarkBtn_Click"/>
                        
                        <TextBlock Text="Video Filter" FontSize="11" Foreground="{StaticResource TextBrush}" Margin="0,5,0,5"/>
                        <ComboBox x:Name="FilterSelector" SelectedIndex="0" Background="#1A1F2B" Foreground="White" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}" Margin="0,0,0,15">
                            <ComboBoxItem Content="None"/>
                            <ComboBoxItem Content="Cinematic Warm"/>
                            <ComboBoxItem Content="Cyberpunk (Cold)"/>
                            <ComboBoxItem Content="Black &amp; White"/>
                        </ComboBox>

                        <TextBlock Text="Audio Synthesis" FontSize="11" Foreground="{StaticResource TextBrush}" Margin="0,5,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Content="Local File" Style="{StaticResource StudioBtn}" Click="SelectMusic_Click" Margin="0,0,4,0"/>
                            <Button Grid.Column="1" Content="YT Studio" Style="{StaticResource StudioBtn}" Click="YTStudioMusic_Click" Margin="4,0,0,0"/>
                        </Grid>
                        <TextBlock x:Name="MusicInfoLabel" Text="No background music" FontSize="10" Foreground="{StaticResource SubTextBrush}" Margin="0,5,0,0"/>

                        <!-- Footer Actions -->
                        <StackPanel Margin="0,40,0,0">
                            <Button x:Name="ExportButton" Content="EXPORT VIDEO" Style="{StaticResource ExportBtn}" Height="40" Click="ExportButton_Click"/>
                            <Button Content="Close Editor" Style="{StaticResource StudioBtn}" Height="35" Margin="0,10,0,0" Click="Close_Click"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Preview & Timeline Area -->
            <Grid Grid.Column="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>      <!-- Video Preview -->
                    <RowDefinition Height="Auto"/>   <!-- Timeline -->
                </Grid.RowDefinitions>

                <!-- Video Display -->
                <Border Grid.Row="0" Background="Black" CornerRadius="8" Margin="0,0,0,20">
                    <Grid>
                        <MediaElement x:Name="VideoPreview" LoadedBehavior="Manual" ScrubbingEnabled="True" Volume="0.5"/>
                        
                        <!-- Watermark Preview Overlay -->
                        <TextBlock x:Name="WatermarkOverlay" Text="ZeroMix Studio" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="15" 
                                   Foreground="White" Opacity="0.3" FontSize="14" FontWeight="Bold" Visibility="Collapsed"/>

                        <!-- Center Play Icon (if paused) -->
                        <Button x:Name="PlayToggleBtn" Content="▶" FontSize="32" Click="TogglePlay_Click" 
                                Background="Transparent" BorderThickness="0" Foreground="White" 
                                HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                    </Grid>
                </Border>

                <!-- Simple Timeline -->
                <StackPanel Grid.Row="1">
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="TIMELINE: TRIM DURASI" FontSize="10" FontWeight="Bold" Foreground="{StaticResource SubTextBrush}"/>
                        <TextBlock x:Name="DurationText" Text="00:00 / 00:00" HorizontalAlignment="Right" FontSize="10" Foreground="{StaticResource SubTextBrush}"/>
                    </Grid>
                    
                    <Border Background="#12151C" Height="10" CornerRadius="5" Margin="0,0,0,10">
                        <Grid>
                            <Slider x:Name="TimelineSlider" Minimum="0" Maximum="100" Margin="-5,0" 
                                    ValueChanged="TimelineSlider_ValueChanged" />
                        </Grid>
                    </Border>

                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Import Video" Style="{StaticResource StudioBtn}" Click="ImportVideo_Click" Margin="0,0,10,0"/>
                            <TextBlock x:Name="FilePathLabel" Text="No file selected" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource SubTextBrush}"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="START:" VerticalAlignment="Center" FontSize="9" Margin="0,0,5,0"/>
                            <TextBox x:Name="StartTimeTxt" Width="50" Text="00:00" Background="#1A1F2B" Foreground="White" BorderThickness="1" BorderBrush="#1F2430" TextAlignment="Center"/>
                            <TextBlock Text="END:" VerticalAlignment="Center" FontSize="9" Margin="15,0,5,0"/>
                            <TextBox x:Name="EndTimeTxt" Width="50" Text="00:00" Background="#1A1F2B" Foreground="White" BorderThickness="1" BorderBrush="#1F2430" TextAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
