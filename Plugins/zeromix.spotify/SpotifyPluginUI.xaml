<UserControl x:Class="ZeroMix.Plugins.Spotify.SpotifyPluginUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="../Styles.xaml"/>
    </UserControl.Resources>
    
    <StackPanel>
        <Border x:Name="SpotifyCard" Style="{StaticResource CompactCardBorder}" MouseDown="SpotifyCard_MouseDown" Cursor="Hand">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icon -->
                <Border Grid.Column="0" Width="42" Height="42" CornerRadius="10" Background="#141922" Margin="0,0,14,0">
                    <TextBlock Text="&#xE8D6;" FontFamily="Segoe MDL2 Assets" FontSize="20"
                               Foreground="#FF69B4"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Spotify Sidebar" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,0,3"/>
                    <TextBlock Text="Premium pink-themed Spotify controller." Foreground="{StaticResource SubTextBrush}" FontSize="11"/>

                    <!-- Progress -->
                    <StackPanel x:Name="PluginProgressPanel" Visibility="Collapsed" Margin="0,10,0,0">
                        <TextBlock x:Name="PluginStatusText" Text="Processing Plugin... 0%" Foreground="#FF69B4" FontSize="11" Margin="0,0,0,5"/>
                        <ProgressBar x:Name="PluginProgressBar" Height="4" Maximum="100" Foreground="#FF69B4" Background="#141922" BorderThickness="0"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox x:Name="SpotifyPluginToggle" VerticalAlignment="Center" Click="SpotifyPluginToggle_Click"
                              Style="{StaticResource ModernCheckBox}"/>
                </StackPanel>
            </Grid>
        </Border>
    </StackPanel>
</UserControl>
