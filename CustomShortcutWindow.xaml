<Window x:Class="ZeroMix.CustomShortcutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Custom Shortcuts"
    Width="850"
    Height="650"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    Icon="/zeromix.ico"
    MouseLeftButtonDown="Window_MouseLeftButtonDown"
    WindowStartupLocation="CenterScreen"
    Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Modern Dark Theme with Glassmorphism -->
        <Color x:Key="BgDark">#1A1A2E</Color>
        <Color x:Key="BgGradientStart">#1A1A2E</Color>
        <Color x:Key="BgGradientEnd">#16213E</Color>
        <Color x:Key="Accent">#00C9FF</Color>
        <Color x:Key="AccentSecondary">#92FE9D</Color>
        <Color x:Key="Success">#2ECC71</Color>
        <Color x:Key="Danger">#E74C3C</Color>
        <Color x:Key="TextPrimary">#E8E8E8</Color>
        <Color x:Key="TextSecondary">#A0A0A0</Color>
        <Color x:Key="GlassPanel">#1AFFFFFF</Color>
        <Color x:Key="CardBg">#252A3F</Color>

        <!-- Brushes -->
        <LinearGradientBrush x:Key="BgGradientBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="{StaticResource BgGradientStart}" Offset="0"/>
            <GradientStop Color="{StaticResource BgGradientEnd}" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource Accent}" />
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource Success}" />
        <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource Danger}" />
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}" />
        <SolidColorBrush x:Key="GlassBrush" Color="{StaticResource GlassPanel}" />
        <SolidColorBrush x:Key="CardBgBrush" Color="{StaticResource CardBg}" />

        <!-- Base Font -->
        <Style TargetType="Control">
            <Setter Property="FontFamily" Value="'Segoe UI', Inter" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        </Style>


        <!-- ComboBox Toggle Button -->
        <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="ClickMode" Value="Press"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="templateRoot" Background="{StaticResource CardBgBrush}" BorderBrush="#3A3F5C" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <Border x:Name="splitBorder" BorderBrush="Transparent" BorderThickness="0" HorizontalAlignment="Right" Margin="0" SnapsToDevicePixels="true" Width="18">
                                <Path x:Name="arrow" Data="F1 M 0,0 L 2.667,2.66665 L 5.3334,0 L 5.3334,-1.78168 L 2.6667,0.88501 L0,-1.78168 L0,0 Z" Fill="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0" VerticalAlignment="Center"/>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="templateRoot" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Item -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderThickness="0" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" TargetName="Bd" Value="#3A3F5C"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" TargetName="Bd" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- Modern Button with Glow Effect -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Padding="{TemplateBinding Padding}" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect x:Name="glowEffect" 
                                                Color="{Binding Background.(SolidColorBrush.Color), RelativeSource={RelativeSource TemplatedParent}}" 
                                                BlurRadius="0" 
                                                Opacity="0" 
                                                ShadowDepth="0"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.95" />
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="border" 
                                                           Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" 
                                                           To="1.03" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="border" 
                                                           Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" 
                                                           To="1.03" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect" 
                                                           Storyboard.TargetProperty="BlurRadius" 
                                                           To="16" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect" 
                                                           Storyboard.TargetProperty="Opacity" 
                                                           To="0.7" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="border" 
                                                           Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" 
                                                           To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="border" 
                                                           Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" 
                                                           To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect" 
                                                           Storyboard.TargetProperty="BlurRadius" 
                                                           To="0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect" 
                                                           Storyboard.TargetProperty="Opacity" 
                                                           To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButton" BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SuccessBrush}" />
        </Style>
        <Style x:Key="DangerButton" BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DangerBrush}" />
        </Style>
        <Style x:Key="SecondaryButton" BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
            <Setter Property="Background" Value="#3A3F5C" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        </Style>

        <!-- Close Button -->
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#888888" />
            <Setter Property="Width" Value="46" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="Transparent" CornerRadius="0,8,0,0">
                            <Path x:Name="xpath" 
                                  HorizontalAlignment="Center" 
                                  VerticalAlignment="Center" 
                                  Data="M0,0 L10,10 M10,0 L0,10" 
                                  Stroke="{TemplateBinding Foreground}" 
                                  StrokeThickness="2" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123" />
                                <Setter TargetName="xpath" Property="Stroke" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Styles -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="RowDetailsVisibilityMode" Value="VisibleWhenSelected" />
            <Setter Property="GridLinesVisibility" Value="None" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="RowHeight" Value="48"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="16,12" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="BorderBrush" Value="#3A3F5C" />
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A2F45" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#1A00C9FF" Offset="0"/>
                                <GradientStop Color="#0A00C9FF" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="0" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>


    <!-- Main Window Border with Shadow -->
    <Border Margin="12" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Opacity="0.4" ShadowDepth="0" Color="#000000" />
        </Border.Effect>
        <Border x:Name="MainBorder" 
                Background="{StaticResource BgGradientBrush}" 
                BorderBrush="#3A3F5C" 
                BorderThickness="1" 
                CornerRadius="12" 
                ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" /> <!-- Title Bar -->
                    <RowDefinition Height="*" />    <!-- Main Content -->
                </Grid.RowDefinitions>

                <!-- Custom Title Bar with Gradient -->
                <Grid Grid.Row="0">
                    <Grid.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#1F2937" Offset="0"/>
                            <GradientStop Color="#252A3F" Offset="1"/>
                        </LinearGradientBrush>
                    </Grid.Background>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Icon -->
                    <Border Grid.Column="0" 
                            Width="32" Height="32" 
                            Margin="16,0,12,0" 
                            CornerRadius="6"
                            Background="{StaticResource AccentBrush}">
                        <TextBlock Text="⚡" 
                                   FontSize="18" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </Border>

                    <!-- Title -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Custom Shortcuts" 
                                   FontSize="15" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimaryBrush}" />
                        <TextBlock Text="Manage your keyboard shortcuts" 
                                   FontSize="11" 
                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                   Margin="0,2,0,0"/>
                    </StackPanel>

                    <!-- Close Button -->
                    <Button Name="CancelButton" 
                            Grid.Column="2" 
                            Click="CancelButton_Click" 
                            Style="{StaticResource CloseButton}" 
                            ToolTip="Close" />
                </Grid>

                <!-- Main Content Grid -->
                <Grid Grid.Row="1" Margin="20">
                    <Grid.RowDefinitions> 
                        <RowDefinition Height="Auto" /> <!-- Input Form -->
                        <RowDefinition Height="*" />    <!-- Data Table -->
                        <RowDefinition Height="Auto" /> <!-- Action Buttons -->
                    </Grid.RowDefinitions>

                    <!-- Input Area Card -->
                    <Border Grid.Row="0" 
                            Margin="0,0,0,20" 
                            Padding="24" 
                            Background="{StaticResource CardBgBrush}" 
                            BorderBrush="#3A3F5C" 
                            BorderThickness="1" 
                            CornerRadius="10">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" Opacity="0.2" ShadowDepth="2" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <!-- Header -->
                            <TextBlock Text="➕ Add New Shortcut" 
                                       FontSize="16" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,16"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- Shortcut Key Input -->
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,0">
                                    <TextBlock Margin="0,0,0,8" 
                                               FontWeight="Medium" 
                                               FontSize="12"
                                               Text="⌨️ Shortcut Key" 
                                               Foreground="{StaticResource TextSecondaryBrush}" />
                                    <Border Background="{StaticResource CardBgBrush}" CornerRadius="6" BorderBrush="#3A3F5C" BorderThickness="1.5">
                                        <TextBox Name="HotkeyTextBox" 
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 Padding="12,10"
                                                 Foreground="{StaticResource TextPrimaryBrush}"
                                                 CaretBrush="{StaticResource AccentBrush}"
                                                 FontSize="13"
                                                 PreviewKeyDown="HotkeyTextBox_PreviewKeyDown" 
                                                 Text="Click and press key combination" />
                                    </Border>
                                </StackPanel>

                                <!-- Application Path Input -->
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="12,0,0,0">
                                    <TextBlock Margin="0,0,0,8" 
                                               FontWeight="Medium" 
                                               FontSize="12"
                                               Text="📁 Application Path" 
                                               Foreground="{StaticResource TextSecondaryBrush}" />
                                    <Grid Grid.Row="1" Grid.Column="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <ComboBox Name="AppPathComboBox" 
                                                  Style="{StaticResource ModernComboBox}"
                                                  Margin="0,0,8,16" 
                                                  DisplayMemberPath="Name" 
                                                  IsTextSearchEnabled="True" 
                                                  StaysOpenOnEdit="True" 
                                                  TextSearch.TextPath="Name" 
                                                  SelectedValuePath="Path" 
                                                  DropDownClosed="AppPathComboBox_DropDownClosed" />
                                        <Button Name="BrowseButton" 
                                                Grid.Column="1" 
                                                Margin="0,0,0,16" 
                                                Click="BrowseButton_Click" 
                                                Content="📂 Browse" 
                                                Style="{StaticResource SecondaryButton}" />
                                    </Grid>
                                </StackPanel>
                            </Grid>

                            <!-- Action Buttons -->
                            <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
                                <Button Name="AddShortcutButton" 
                                        Width="140" 
                                        Click="AddShortcutButton_Click" 
                                        Content="✓ Add Shortcut" 
                                        Style="{StaticResource SuccessButton}" />
                                <Button Name="DeleteShortcutButton" 
                                        Width="140" 
                                        Margin="12,0,0,0" 
                                        Click="DeleteShortcutButton_Click" 
                                        Content="✕ Delete Selected" 
                                        Style="{StaticResource DangerButton}" />
                                <Border Width="20"/>
                                <Border Background="#2A2F45" 
                                        CornerRadius="4" 
                                        Padding="12,8">
                                    <TextBlock VerticalAlignment="Center" 
                                               FontSize="11"
                                               Foreground="{StaticResource TextSecondaryBrush}">
                                        <Run Text="💡" FontSize="13"/>
                                        <Run Text="Tip: Select a row before deleting"/>
                                    </TextBlock>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Shortcut List Card -->
                    <Border Grid.Row="1" 
                            Background="{StaticResource CardBgBrush}" 
                            BorderBrush="#3A3F5C" 
                            BorderThickness="1" 
                            CornerRadius="10"
                            Margin="0,0,0,20">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" Opacity="0.2" ShadowDepth="2" Color="#000000"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- List Header -->
                            <Border Grid.Row="0" 
                                    Background="#1F2937" 
                                    Padding="16,12"
                                    CornerRadius="10,10,0,0">
                                <TextBlock Text="📋 Your Shortcuts" 
                                           FontSize="14" 
                                           FontWeight="SemiBold" 
                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                            </Border>

                            <!-- DataGrid -->
                            <DataGrid Name="ShortcutListView" 
                                      Grid.Row="1" 
                                      Margin="0" 
                                      AutoGenerateColumns="False" 
                                      CanUserAddRows="False" 
                                      CanUserDeleteRows="False" 
                                      IsReadOnly="True" 
                                      ItemsSource="{Binding Shortcuts}" 
                                      SelectionMode="Single">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Width="220" 
                                                        Binding="{Binding Hotkey}" 
                                                        Header="⌨️ Shortcut" />
                                    <DataGridTextColumn Width="*" 
                                                        Binding="{Binding ApplicationPath}" 
                                                        Header="📁 Application" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                    <!-- Save Button -->
                    <StackPanel Grid.Row="2" 
                                HorizontalAlignment="Right" 
                                Orientation="Horizontal">
                        <Button Name="SaveButton" 
                                Width="160" 
                                Height="40"
                                Click="SaveButton_Click" 
                                Content="💾 Save and Apply" 
                                FontSize="14"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Border>

    <!-- Window entrance animation -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                     From="0" 
                                     To="1" 
                                     Duration="0:0:0.3">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <ThicknessAnimation Storyboard.TargetProperty="Margin" 
                                        From="12,30,12,12" 
                                        To="12,12,12,12" 
                                        Duration="0:0:0.3">
                        <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </ThicknessAnimation.EasingFunction>
                    </ThicknessAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
