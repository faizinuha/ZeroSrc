<Window x:Class="ZeroSrc.SearchOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ZeroSrc"
        Title="ZeroSrc"
        Height="90"
        Width="420"
        Topmost="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Opacity="0"
        Loaded="Window_Loaded">
    <Window.Resources>
        <!-- Storyboard untuk efek Fade In dan Pop In yang lebih halus -->
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0"
                             To="1"
                             Duration="0:0:0.25"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="0.95"
                             To="1"
                             Duration="0:0:0.25">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase Exponent="5" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="0.95"
                             To="1"
                             Duration="0:0:0.25">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase Exponent="5" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Storyboard untuk efek Fade Out yang lebih cepat -->
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1"
                             To="0"
                             Duration="0:0:0.15"/>
        </Storyboard>

        <!-- Efek bayangan yang lebih halus dan modern -->
        <DropShadowEffect x:Key="Shadow"
                          BlurRadius="20"
                          ShadowDepth="4"
                          Direction="270"
                          Color="#1E293B"
                          Opacity="0.1"/>

        <!-- Gradient untuk logo, menggunakan warna yang lebih cerah -->
        <LinearGradientBrush x:Key="ZGradient"
                             StartPoint="0,0"
                             EndPoint="1,1">
            <GradientStop Color="#4285F4" Offset="0.0"/>
            <GradientStop Color="#34A853" Offset="0.33"/>
            <GradientStop Color="#FBBC05" Offset="0.66"/>
            <GradientStop Color="#EA4335" Offset="1.0"/>
        </LinearGradientBrush>

        <!-- Style untuk TextBox agar terlihat lebih modern -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,0,8,0"/>
            <Setter Property="Foreground" Value="#2D3748"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="CaretBrush" Value="#4A5568"/>
        </Style>

        <!-- Template untuk Placeholder Text -->
        <ControlTemplate x:Key="SearchBoxPlaceholderTemplate" TargetType="{x:Type TextBox}">
            <Grid>
                <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True"/>
                <ScrollViewer x:Name="PART_ContentHost" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                <TextBlock x:Name="Placeholder"
                           Margin="{TemplateBinding Padding}"
                           Text="Type here to search..."
                           Foreground="#A0AEC0"
                           FontStyle="Italic"
                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                           IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static local:StringToVisibilityConverter.Instance}}"
                                             Value="Collapsed">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </ControlTemplate>

        <!-- Style untuk item ListBox -->
        <Style x:Key="SuggestionItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Background" Value="#FFF"/>
            <Setter Property="Foreground" Value="#2D3748"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E2E8F0"/>
                                <Setter Property="Foreground" Value="#1A202C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.RenderTransform>
        <ScaleTransform ScaleX="1" ScaleY="1" CenterX="210" CenterY="45"/>
    </Window.RenderTransform>

    <Border CornerRadius="16"
            Background="#F7FAFC"
            Effect="{StaticResource Shadow}"
            Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Viewbox Grid.Column="0"
                         Margin="12,0,0,0"
                         VerticalAlignment="Center"
                         Height="40"
                         Width="40">
                    <Canvas Width="32" Height="32">
                        <Path Data="M 4,6 L 28,6 L 4,26 L 28,26"
                              StrokeThickness="5"
                              Stroke="{StaticResource ZGradient}"
                              StrokeLineJoin="Round"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"/>
                    </Canvas>
                </Viewbox>

                <TextBox Name="SearchBox"
                         Grid.Column="1"
                         Margin="0,16,16,16"
                         KeyDown="SearchBox_KeyDown"
                         TextChanged="SearchBox_TextChanged"
                         Style="{StaticResource SearchBoxStyle}"
                         Template="{StaticResource SearchBoxPlaceholderTemplate}"/>
            </Grid>

            <ListBox Name="SuggestionList"
                     Grid.Row="1"
                     Margin="60,0,16,16"
                     MaxHeight="200"
                     Background="Transparent"
                     BorderThickness="0"
                     Visibility="Collapsed"
                     ItemsSource="{Binding Suggestions}"
                     SelectionChanged="SuggestionList_SelectionChanged"
                     MouseDoubleClick="SuggestionList_MouseDoubleClick"
                     KeyDown="SuggestionList_KeyDown"
                     ItemContainerStyle="{StaticResource SuggestionItemStyle}"/>

            <TextBlock Name="NotificationText"
                       Grid.Row="2"
                       Margin="60,0,16,16"
                       TextAlignment="Center"
                       FontWeight="SemiBold"
                       Foreground="#6B46C1"
                       Visibility="Collapsed"/>
        </Grid>
    </Border>
</Window>
