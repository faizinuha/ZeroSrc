<Window x:Class="ZeroMix.Plugins.DynamicPluginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ZeroMix Plugin" Height="250" Width="250"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="False">

    <Border Background="White" CornerRadius="10" BorderBrush="#EEEEEE" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.1"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/> <!-- Smaller Title Bar -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" Background="#FBFBFB" MouseLeftButtonDown="TitleBar_MouseDown">
                <TextBlock x:Name="TitleText" Text="Plugin" FontWeight="Bold" Foreground="#666" FontSize="11" VerticalAlignment="Center" Margin="12,0"/>
                <Button Content="✕" Click="Close_Click" HorizontalAlignment="Right" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" Foreground="#AAA" Margin="8,0" Cursor="Hand" FontSize="10"/>
            </Grid>

            <!-- Dinamic Content Container -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="MainContainer" Margin="20"/>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
