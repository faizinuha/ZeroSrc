<Window x:Class="ZeroMix.Plugins.Welcome.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:gif="clr-namespace:WpfAnimatedGif;assembly=WpfAnimatedGif"
        Title="Welcome"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        Width="420" Height="420"
        ResizeMode="NoResize">

    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="0.85" To="1" Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="0.85" To="1" Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        <Storyboard x:Key="FadeOut" Completed="FadeOut_Completed">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.5"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="1" To="0.9" Duration="0:0:0.5"/>
        </Storyboard>
    </Window.Resources>

    <Window.RenderTransform>
        <ScaleTransform CenterX="210" CenterY="210"/>
    </Window.RenderTransform>

    <Grid>
        <!-- Blur backdrop -->
        <Border CornerRadius="24" Margin="10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#E80A0D13" Offset="0"/>
                    <GradientStop Color="#E8141922" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.6" Color="Black"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- GIF -->
                <Image x:Name="WelcomeGif"
                       Width="220" Height="220"
                       Margin="0,20,0,10"
                       gif:ImageBehavior.RepeatBehavior="Forever"/>

                <!-- Welcome text -->
                <TextBlock x:Name="WelcomeTitle"
                           Text="Welcome back"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,6"/>
                <TextBlock x:Name="WelcomeSubtitle"
                           Text="ZeroMix is ready."
                           FontSize="13"
                           Foreground="#88FFFFFF"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,24"/>

                <!-- Dismiss hint -->
                <TextBlock Text="Click anywhere to dismiss"
                           FontSize="10"
                           Foreground="#44FFFFFF"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,16"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
